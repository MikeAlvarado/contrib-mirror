## What this changes

## Why

## Checklist

- [ ] Tests added or updated in `tests/`
- [ ] `python -m unittest discover -s tests` passes
- [ ] `README.md` and `CHANGELOG.md` updated if inputs, outputs, or behavior changed
- [ ] Only dates are stored, the run still fails loud, and nothing pushes
- [ ] No em dashes, no inline comments in commands or YAML
